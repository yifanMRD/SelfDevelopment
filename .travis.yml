sudo: false

language: r
r:
  - oldrel
  - release
  - devel

cache: packages

install:
  - Rscript -e 'install.packages(c("devtools","roxygen2","testthat"));devtools::install_deps("/var/folders/t9")'
script:
  - Rscript -e 'devtools::check("/var/folders/t9")'